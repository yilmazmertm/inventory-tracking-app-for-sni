package com.yilmazmertm;

public class CreateSampleData {

    public static void main(String[] args) {
        //SessionFactory factory = new Configuration().configure("hibernate.cfg.xml").addAnnotatedClass(Product.class).addAnnotatedClass(User.class).buildSessionFactory();
        //Session session = factory.getCurrentSession();

        try {
            //Product2 product2 = new Product2();
            //product2.setProductId(50);
            //product2.setAntivirus("var");
            //product2.setBilgisayarAdi("yeni bilgisayar");
            //User user = new User();
            //user.setId(99);
            //user.setUserName("Deneme");
            //product2.setUser(user);
            //session.beginTransaction();
            //session.save(product2);
            //session.save(user);
            //session.getTransaction().commit();
        } finally {
            //factory.close();
            //session.close();
        }
    }
}
